spring.datasource.url=jdbc:postgresql://localhost:5432/flags
spring.datasource.username=flags
spring.datasource.password=flags

# Create the table and seed flags on startup (idempotent).
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:db/schema.sql
